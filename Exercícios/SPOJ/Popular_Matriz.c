#include <stdio.h>

int main()
{
    int n, matriz_1[n][n];
    scanf("%i", n);

    for (int i = 1; i < n; i++)
    {
        for (int j = 1; j < n; j++)
        {
            if (matriz[j][i] = )
            {
                
            }
        }
    }

    return 0;
}
