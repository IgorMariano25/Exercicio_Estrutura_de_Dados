#include <stdio.h>
int main(){
    int n, soma;
    scanf("%i", &n);
    if (n < 1){
        printf("Valor de n é menor que 1, por isso não é possível imprimir os números no intervalo")
    }
    for()
}